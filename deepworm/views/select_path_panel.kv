#:kivy 1.0

<SelectPathPanel>:
    id:select_path_panel
    Button:
        id:btn
        text: 'File'
        on_press: root.open_file()
    Button:
        text: 'Folder'
        on_press: root.open_folder()
