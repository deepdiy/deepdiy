<ModelItemButton@Button>:
	on_release:root.dropdown.select(self.text)
	size_hint_y:None
	height:dp(50)

<Run>:
	orientation: 'vertical'
	Button:
		id:btn
		text:'Please select a model'
		on_release:dropdown.open(self)
		size_hint_y:None
		height:dp(50)

	DropDown:
		id:dropdown
		on_select:btn.text='Model: {}'.format(args[1])
		# Button:
		# 	text: 'File'
		# 	on_release:dropdown.select('A')
		# 	size_hint_y:None
		# 	height:dp(50)
		# Button:
		# 	text: 'Folder'
		# 	on_release:dropdown.select('B')
		# 	size_hint_y:None
		# 	height:dp(50)
	Button:
		id:btn_detect_run
		text:'run'
		size_hint_y:None
		height:dp(50)
