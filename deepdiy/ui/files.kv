#:kivy 1.0

<Files>:
    id:select_path_panel
    Button:
        id:btn
        text: 'File'
        size_hint_y:None
        height:dp(40)
        on_release: root.open_file()
    Button:
        text: 'Folder'
        size_hint_y:None
        height:dp(40)
        on_release: root.open_folder()
